const body = document.querySelector("body");
const tvs = document.querySelector("#tvs .product-list");
const phones = document.querySelector("#phones .product-list");
const laptops = document.querySelector("#laptops .product-list");
const formContainer = document.querySelector(".form-container");
const form = document.querySelector(".form");
const modalCloseButton = document.querySelector(".button_red");
const anchors = document.querySelectorAll("a");
const colorThemeToggle = document.querySelector(".header__color-mode-switch");
const radioButtons = document.querySelectorAll(".form .radio");
const formInput = document.querySelector(".form__input");
const formButton = document.querySelector("form .button");
const upButton = document.querySelector(".button_up");
const navBarButton = document.querySelector(".nav-bar");
const navBarMenu = document.querySelector(".nav-bar__menu");
const footer = document.querySelector(".footer");
const footerButton = document.querySelector(".button_footer");
export default {
  body,
  tvs,
  phones,
  laptops,
  formContainer,
  form,
  modalCloseButton,
  anchors,
  colorThemeToggle,
  radioButtons,
  formInput,
  upButton,
  navBarButton,
  navBarMenu,
  footer,
  footerButton,
  formButton,
};
